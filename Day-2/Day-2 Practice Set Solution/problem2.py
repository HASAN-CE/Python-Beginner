# Prompt the user to input a number and a divisor
num = int(input('Enter the number: '))
Z = int(input('Enter the divisor: '))

# Calculate the remainder
remainder = num % Z

# Output the remainder
print(f'Remainder when {num} is divided by {Z} is {remainder}')
